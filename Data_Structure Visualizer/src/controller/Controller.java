package controller;

import model.Model;

public class Controller {

	private Model model = new Model();
	private int line = 0;
	private int language = 0;
	
	//line 입력받는 메소드(line 값이 정확한지 검사 필요)
	//language 입력받는 메소드(java는 -1 C는 -2로 변환)
	//입력 코드 가져오는 메소드
	//변수명 가져오는 메소드
	//컴파읿 버튼 눌렀을시 language, line, varname, code Model로 보내는 메소드(이미지 선택까지 하게?)
	//prev, next 기능 메소드(버튼 눌렀을때 새로 컴파일 하는 것보다 디버거 기능으로 앞뒤로 옮겨가는 기능을 이용해서 구현하는 것이 좋은 )
		
}
